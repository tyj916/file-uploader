<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= file.name %></title>
</head>
<body>
  <%- include('./partials/nav.ejs') %>

  <main>
    <a href="/folder/<%= file.folderId %>" class="folder-nav">&lt; Back to folder</a>

    <div class="container">
      <h1><%= file.name %></h1>
      <h2 class="info-title">General Information</h2>
      <h3>Upload Time</h3>
      <p><%= new Date(file.uploadTime).toLocaleString('en-SG') %></p>
      <h3>Size</h3>
      <p><%= file.size %> bytes</p>
    
      <div class="buttons-horizontal">
        <button type="button">
          <a href="/folder/<%= file.folderId %>/file/<%= file.id %>/edit">Edit</a>
        </button>
        <form action="/folder/<%= file.folderId %>/file/<%= file.id %>/delete" method="POST">
          <button 
            type="submit" 
            onclick="return confirm('Are you sure you want to delete <%= file.name %>?')"
          >
            Delete File
          </button>
        </form>
        <form action="/folder/<%= file.folderId %>/file/<%= file.id %>/download" method="POST">
          <button type="submit">Download</button>
        </form>
      </div>
    </div>
  </main>
</body>
</html>