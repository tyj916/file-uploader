<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title><%= file.name %></title>
</head>
<body>
  <%- include('./partials/nav.ejs') %>

  <a href="/folder/<%= file.folderId %>">&lt; Back to folder</a>

  <h1><%= file.name %></h1>
  <h2>General Information</h2>
  <h3>Upload Time</h3>
  <p><%= new Date(file.uploadTime).toLocaleString('en-SG') %></p>
  <h3>Size</h3>
  <p><%= file.size %> bytes</p>

  <a href="/folder/<%= file.folderId %>/file/<%= file.id %>/edit">Edit</a>
  <form action="/folder/<%= file.folderId %>/file/<%= file.id %>/delete" method="POST">
    <button 
      type="submit" 
      onclick="return confirm('Are you sure you want to delete <%= file.name %>?')"
    >
      Delete File
    </button>
  </form>
</body>
</html>